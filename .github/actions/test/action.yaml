name: Test
description: Run unit test suite

runs:
  using: composite
  steps:
    - name: Test
      shell: bash
      run: |
        echo "### :microscope: Running tests..." >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        ./test >> $GITHUB_STEP_SUMMARY 2>&1
        echo '```' >> $GITHUB_STEP_SUMMARY
